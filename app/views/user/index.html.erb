</script>
<div class="center-block">
    <h3 class="center-block">User一覧</h3>
    <table class="table table-border">
        <thead>
            <tr>
                <th>email</th>
                <th>status</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            <% @userInfo.each do |info| %>
                <tr>
                    <td>
                        <%= info.email %>
                    </td>
                    <td>
                        <% if info.locked_at.nil? %>
                            <span class="account_status">ロック</span>
                        <% else %>
                            <span class="account_status">アンロック</span>
                        <% end %>
                    </td>
                    <td>
                        <button class="btn btn-warning" data-toggle="modal" data-target="#change_account_status" data-whatever=info>アカウントステータス変更</span>
                    </td>
                </tr>
            <% end %>
        </tbody>
    </table>
    <div class="modal fade" id="change_account_status">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                    <h4>アカウントステータスの変更</h4>
                </div>
                <div class="modal-body">
                    <p>対象アカウント：<span class="this-modal-account-email"></span></p>
                    <p>ステータス：<span class=""
                       
                    </p>
                </div>
                <div class="modal-footer">
                    <button class="btn btn-success" data-dismiss="modal" aria-label="Close"">OK</button>
                    <button class="btn btn-default" data-dismiss="modal" aria-label="Close">cancel</button>
                </div>
            </div>
        </div>
    </div>

</div>